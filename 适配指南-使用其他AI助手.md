# 适配指南：使用其他AI助手

## ✅ 系统兼容性

**好消息**：Vibe Writing 系统**不依赖 Claude 特定功能**，可以用任何支持以下功能的AI助手：

### 必需功能
1. **文件读写操作**：Read、Write、Edit 文件
2. **终端命令**：执行 `pwd`、`mkdir` 等命令
3. **WebSearch**：进行事实核查（可选，但推荐）
4. **长上下文**：能够读取和理解多个Agent规则文件

### 支持的AI助手
- ✅ **GPT-4 / GPT-4o**（ChatGPT Plus、Cursor、其他支持GPT-4的环境）
- ✅ **Gemini Pro / Ultra**（Google AI Studio、Cursor等）
- ✅ **其他支持文件操作的AI助手**

---

## 🚀 快速开始（使用其他AI助手）

### 方法1：直接让AI读取系统文件

1. **打开你的AI助手**（ChatGPT、Cursor、或其他）

2. **在项目根目录启动对话**，然后发送：

```
请读取并理解这个文件：vibe-writing-Tasihi/CLAUDE.md

这是一个思维协同写作系统，我需要你按照这个系统的规则来帮助我创作。

现在，我想写关于"[你的主题]"
```

3. **AI会自动加载系统规则**，然后按照工作流程执行

### 方法2：分步引导

如果AI无法一次性理解整个系统，可以分步引导：

**第一步**：让AI了解系统
```
请读取以下文件，理解这个写作系统的工作流程：
1. vibe-writing-Tasihi/CLAUDE.md
2. vibe-writing-Tasihi/_系统/学习Agent.md
3. vibe-writing-Tasihi/_系统/规范/核心规范.md
```

**第二步**：开始使用
```
现在按照学习Agent的规则，帮我开始一个新项目。
我想写关于"[你的主题]"
```

---

## 📋 使用流程（通用版）

### 1️⃣ 启动项目

**对AI说**：
```
我想写关于"[你的主题]"
```

**AI会**：
- 读取 `CLAUDE.md` 和 `学习Agent.md`
- 创建项目文件夹
- 生成初始文档
- 开始学习阶段的对话

### 2️⃣ 学习阶段

**直接提问和讨论**，AI会：
- 每4轮对话自动生成知识卡片
- 或说 `/生成卡片` 立即生成

### 3️⃣ 结构阶段

**对AI说**：
```
/结构
```
或
```
请按照结构Agent的规则，分析知识卡片并生成输出卡片
```

**AI会**：
- 读取 `结构Agent.md`
- 分析知识卡片
- 提供结构方案
- 生成输出卡片

### 4️⃣ 迭代阶段

**对AI说**：
```
/迭代
```
或
```
请按照写作Agent的规则，优化这个输出卡片：[卡片路径]
```

**AI会**：
- 读取 `写作Agent.md`
- 进入迭代或润色模式

### 5️⃣ 成稿阶段

**对AI说**：
```
/成稿
```
或
```
请按照成稿Agent的规则，将所有输出卡片串联成完整文章
```

**AI会**：
- 读取 `成稿Agent.md`
- 读取白板上的所有输出卡片
- 生成最终成稿

---

## 🔧 关键提示

### 提示1：让AI读取系统文件

每次开始新对话或切换阶段时，明确告诉AI读取对应的Agent文件：

```
请读取 vibe-writing-Tasihi/_系统/学习Agent.md 并按照规则执行
```

### 提示2：使用快捷命令

即使AI助手不支持 `/结构` 这样的命令，你可以用自然语言：

- `/结构` → "请进入结构阶段，分析知识卡片"
- `/迭代` → "请进入迭代模式，优化这个卡片"
- `/润色` → "请润色这个文件"
- `/成稿` → "请生成最终成稿"

### 提示3：路径问题

如果AI助手无法自动检测工作目录，可以明确告诉它：

```
工作目录是：/Users/davidwang/Documents/GitHub/vibe-writing-workflow/vibe-writing-Tasihi

所有文件操作使用相对路径：项目/、_系统/
```

### 提示4：Obsidian白板（可选）

如果不用Obsidian，系统仍然可以正常工作：
- 白板文件（`.canvas`）是JSON格式，AI可以直接编辑
- 或者跳过白板组织，直接进入成稿阶段

---

## 🎯 推荐使用方式

### 方案A：Cursor（推荐）

如果你使用 **Cursor IDE**：
1. 打开项目文件夹
2. 在Cursor中启动AI对话
3. 直接说："我想写关于XX"
4. Cursor的AI会自动读取项目文件，理解系统规则

### 方案B：ChatGPT Plus

如果你使用 **ChatGPT Plus**（GPT-4）：
1. 上传或让AI读取 `CLAUDE.md` 文件
2. 告诉AI："请按照这个系统的规则工作"
3. 开始使用

**注意**：ChatGPT的文件读取能力有限，可能需要分步引导

### 方案C：本地AI助手

如果你使用支持文件操作的本地AI：
1. 在项目目录启动
2. 让AI读取系统文件
3. 开始使用

---

## ❓ 常见问题

### Q: AI不理解系统规则怎么办？

**A**: 分步引导：
1. 先让AI读取 `CLAUDE.md`
2. 再读取对应的Agent文件（如 `学习Agent.md`）
3. 明确告诉AI："请严格按照这个文件的规则执行"

### Q: AI不执行文件操作怎么办？

**A**: 明确指示：
```
请使用Write工具创建文件：项目/[项目名]/项目信息.md
```

### Q: 快捷命令不工作怎么办？

**A**: 用自然语言替代：
- `/结构` → "请进入结构阶段"
- `/迭代` → "请进入迭代模式"
- `/润色` → "请润色这个文件"

### Q: 可以修改系统文件吗？

**A**: 可以！系统设计为模块化，你可以：
- 修改Agent规则适应你的需求
- 调整工作流程
- 添加新功能

---

## 📝 示例对话

### 第一次使用

**你**：
```
请读取 vibe-writing-Tasihi/CLAUDE.md 文件，理解这个写作系统。

然后帮我开始一个新项目，我想写关于"为什么AI能理解人类语言"
```

**AI**：
```
[读取文件，理解系统]
[按照学习Agent规则创建项目]
[生成初始文档]
[开始对话引导]
```

### 进入结构阶段

**你**：
```
请读取 vibe-writing-Tasihi/_系统/结构Agent.md

然后分析知识卡片，提供结构方案
```

**AI**：
```
[读取结构Agent规则]
[分析知识卡片]
[提供2-3种结构方案]
```

---

## 🎉 总结

**核心要点**：
1. ✅ 系统不依赖Claude，任何支持文件操作的AI都可以用
2. ✅ 关键是让AI读取和理解系统规则文件
3. ✅ 用自然语言替代快捷命令
4. ✅ 明确告诉AI使用哪个Agent和规则

**开始使用**：
1. 打开你的AI助手
2. 让它读取 `CLAUDE.md`
3. 说出你的主题
4. 开始创作！

---

**祝你创作愉快！** 🚀

